package FileWork;

import FileFind.FileEvent;
import controller.FileWorker;

/**
 * Deleter interacts with the file model and the frame to work on the files
 * activates the Delete button
 *
 */
public class Deleter implements FileWorker {


    @Override
    public void work(FileEvent fileEvent) {

    }
}
